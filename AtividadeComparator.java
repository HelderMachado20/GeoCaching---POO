
/**
 * Esta classe serve para comparar duas datas (GregorianCalendar) e retornar se a data o1 é anterior, igual ou posterior à data o2.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */

import java.util.Comparator;

public class AtividadeComparator implements Comparator
{
    public int compare(Object o1, Object o2) { //Foi colocado este método nesta forma apenas para permitir a compilação
        int res=0;
        return res;
    }
}
